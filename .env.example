# Database Configuration
LIBSQL_URL=file:./db/app.db
LIBSQL_AUTH_TOKEN=

# Cloudflare D1 Configuration (for production)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_API_TOKEN=

# Vite Environment Variables (VITE_* prefix exposed to client)
# These will be available in both client and server code
VITE_API_URL=http://localhost:3000/api
VITE_APP_TITLE=My App
VITE_APP_VERSION=1.0.0

# Server-only Variables (no VITE_* prefix)
# These are only available on the server side
API_SECRET_KEY=
DATABASE_URL=
